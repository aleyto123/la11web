spring:
  application:
    name: citas-service
  profiles:
    active: dev
  datasource:
    url: jdbc:h2:mem:citasdb
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    defer-datasource-initialization: true
    hibernate:
      ddl-auto: update
    show-sql: true
  h2:
    console:
      enabled: true

server:
  port: 8083

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

---
spring:
  config:
    activate:
      on-profile: test
  datasource:
    url: jdbc:h2:mem:citas_test
server:
  port: 9083

---
spring:
  config:
    activate:
      on-profile: prod
  datasource:
    url: jdbc:h2:file:./data/citasdb
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
server:
  port: 8083
